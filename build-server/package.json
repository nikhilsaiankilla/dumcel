{
  "name": "build-server",
  "version": "1.0.0",
  "description": "this is the worker which takes the git url and creates the build folder",
  "main": "script.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@aws-sdk/client-s3": "^3.884.0",
    "@aws-sdk/client-secrets-manager": "^3.893.0",
    "cloudinary": "^2.8.0",
    "kafkajs": "^2.2.4",
    "mime-types": "^3.0.1"
  }
}
